const github = "https://github.com/nordpaul/guardrail-wallet";
const supportAddress = "UQCLbq0UGD65ljHQGlqF5pNgMAWDgE_bnavRxSSRZy-V3rEJ";

const chrome = (lang: string, title: string, description: string, canonical: string, body: string) => `<!doctype html>
<html lang="${lang}"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>${title}</title><meta name="description" content="${description}"><link rel="canonical" href="${canonical}">
<meta property="og:type" content="website"><meta property="og:site_name" content="Guardrail Wallet"><meta property="og:title" content="${title}"><meta property="og:description" content="${description}"><meta property="og:url" content="${canonical}">
<meta name="twitter:card" content="summary"><meta name="twitter:title" content="${title}"><meta name="twitter:description" content="${description}">
<link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&family=Syne:wght@500;600;700;800&display=swap" rel="stylesheet">
<style>
:root{--ink:#172019;--paper:#f3efdf;--acid:#c9ff45;--ember:#f0643b;--white:#fffdf5;--muted:#556057}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--ink);font-family:"DM Mono",monospace;overflow-x:hidden}body:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.22;background-image:linear-gradient(var(--ink) 1px,transparent 1px),linear-gradient(90deg,var(--ink) 1px,transparent 1px);background-size:42px 42px;mask-image:linear-gradient(#000,transparent 75%)}a{color:inherit}.shell{width:min(1180px,calc(100% - 34px));margin:auto;position:relative}.nav{min-height:84px;display:flex;align-items:center;justify-content:space-between;border-bottom:2px solid var(--ink);gap:24px}.mark{font:800 18px Syne,sans-serif;white-space:nowrap}.mark i{display:inline-block;width:13px;height:13px;background:var(--acid);border:2px solid var(--ink);margin-right:8px;transform:rotate(45deg)}.nav nav{display:flex;align-items:center;gap:20px}.nav a{text-decoration:none;font-size:10px;text-transform:uppercase}.nav a:hover,.nav a[aria-current=page]{text-decoration:underline;text-decoration-thickness:2px;text-underline-offset:5px}h1,h2,h3{font-family:Syne,sans-serif;letter-spacing:-.055em;margin:0}h1{font-size:clamp(48px,8vw,105px);line-height:.84}h2{font-size:clamp(34px,5vw,64px);line-height:.93}h3{font-size:25px;line-height:1}.eyebrow{display:inline-flex;align-items:center;gap:8px;border:2px solid var(--ink);background:var(--white);padding:7px 10px;font-size:10px;text-transform:uppercase;box-shadow:4px 4px 0 var(--ink)}.eyebrow b{width:8px;height:8px;background:var(--acid);border-radius:50%}.hero{padding:68px 0 58px;border-bottom:2px solid var(--ink)}.hero-grid{display:grid;grid-template-columns:1.2fr .8fr;gap:45px;align-items:end}.hero p{font-size:14px;line-height:1.8;max-width:600px;margin:25px 0 0}.hero-card{border:2px solid var(--ink);background:var(--acid);padding:24px;box-shadow:8px 8px 0 var(--ink);font-size:12px;line-height:1.8}.hero-card strong{font:700 25px Syne,sans-serif;display:block;line-height:1;margin-bottom:20px}.layout{display:grid;grid-template-columns:225px minmax(0,1fr);gap:42px;padding:58px 0 100px}.toc{align-self:start;position:sticky;top:20px;border:2px solid var(--ink);background:var(--white);padding:17px}.toc b{font:700 11px Syne,sans-serif;text-transform:uppercase}.toc a{display:block;font-size:10px;margin-top:12px;text-decoration:none}.toc a:hover{text-decoration:underline}.content{min-width:0}.content section{padding:0 0 50px;margin-bottom:50px;border-bottom:2px solid var(--ink)}.content section:last-child{border:0}.content p,.content li{font-size:13px;line-height:1.85}.content ul{padding-left:20px}.content h2{margin-bottom:23px}.content h3{margin:31px 0 11px}.callout{border-left:7px solid var(--ember);background:var(--white);padding:18px 20px;margin:25px 0;font-size:12px;line-height:1.75}.callout.safe{border-color:var(--acid);background:#e9f4be}.steps{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin:26px 0}.step{border:2px solid var(--ink);padding:18px;background:var(--white);font-size:11px;line-height:1.7}.step:nth-child(2){background:var(--acid)}.step:nth-child(3){background:var(--ember);color:var(--white)}.n{display:block;font:700 12px Syne,sans-serif;margin-bottom:28px}.code{margin:20px 0;border:2px solid var(--ink);background:#111813;color:#d9e4d9;box-shadow:6px 6px 0 var(--ember);overflow:auto}.code label{display:block;border-bottom:1px solid #607263;padding:9px 14px;color:var(--acid);font-size:10px;text-transform:uppercase}.code pre{margin:0;padding:16px;font:11px/1.7 "DM Mono",monospace;white-space:pre;min-width:max-content}.code .comment{color:#94a093}.table-wrap{overflow:auto;border:2px solid var(--ink);margin:22px 0}.table{border-collapse:collapse;width:100%;min-width:580px;background:var(--white);font-size:11px}.table th,.table td{padding:13px;text-align:left;vertical-align:top;border-bottom:1px solid var(--ink)}.table th{background:var(--acid);font:700 11px Syne,sans-serif}.pill{display:inline-block;border:1px solid var(--ink);padding:2px 5px;background:var(--acid);font-size:10px}.footer{border-top:2px solid var(--ink);padding:28px 0 38px;display:flex;gap:20px;justify-content:space-between;font-size:10px}.footer a{overflow-wrap:anywhere}@media(max-width:780px){.nav{min-height:70px}.nav nav{display:none}.hero{padding:50px 0}.hero-grid,.layout{grid-template-columns:1fr}.layout{gap:26px;padding-top:35px}.toc{position:static}.steps{grid-template-columns:1fr}.content section{padding-bottom:36px;margin-bottom:36px}.footer{flex-direction:column}.hero-card{box-shadow:5px 5px 0 var(--ink)}}
</style></head><body>
<header class="shell nav"><a class="mark" href="/"><i></i>GUARDRAIL WALLET</a><nav><a href="/">Home</a><a href="/docs">Docs</a><a href="/docs/ru">RU</a><a href="/api">API</a><a href="/dashboard">Dashboard</a><a href="${github}">GitHub</a></nav></header>${body}
<footer class="shell footer"><span>Guardrail Wallet / Payment firewall for AI agents</span><a href="https://app.tonkeeper.com/transfer/${supportAddress}?text=Support%20Guardrail%20Wallet">Support: ${supportAddress}</a></footer></body></html>`;

export const docsEnglishHtml: string = chrome("en", "Guardrail Wallet Documentation | Payment firewall for AI agents", "Build bounded AI agent payments with Guardrail Wallet: request, policy, approval and stub execution.", "https://patronhill.ru/docs", `<main>
<header class="shell hero"><div class="hero-grid"><div><span class="eyebrow"><b></b>Documentation / v1</span><h1>PAYMENTS<br>WITH A<br>STOP BUTTON.</h1><p>Guardrail Wallet is a payment firewall for AI agents. Your agent can request a payment; policy and an owner decide whether it proceeds. The agent never receives the owner credential or an execution secret.</p></div><aside class="hero-card"><strong>Start safely</strong>Hosted demo credentials are intentionally public.<br><br>Agent: <code>guardrail-demo-agent</code><br>Owner: <code>guardrail-demo-owner</code><br><br>Demo runs <code>EXECUTOR=stub</code>. It creates records only. No real payments, wallets, balances, or settlement occur.</aside></div></header>
<div class="shell layout"><aside class="toc"><b>On this page</b><a href="#demo">Demo contract</a><a href="#lifecycle">Lifecycle</a><a href="#architecture">Architecture</a><a href="#policies">Policies</a><a href="#docker">Docker</a><a href="#security">Security</a><a href="#troubleshooting">Troubleshooting</a></aside><article class="content">
<section id="demo"><h2>THE DEMO IS A LAB.</h2><p>Open <a href="/dashboard">the dashboard</a> and use the shared credentials to explore a complete payment lifecycle. Send an API request with the agent token, observe the policy outcome, then use the owner token in the dashboard to approve or reject requests that need review. Public demo data can be viewed, altered, or reset by other visitors; do not place personal, commercial, or sensitive data there.</p><div class="callout"><b>Execution boundary:</b> the hosted environment explicitly uses <code>EXECUTOR=stub</code>. A successful demo request means the firewall recorded a simulated execution result. It is not a transfer and cannot move funds.</div></section>
<section id="lifecycle"><h2>REQUEST IS NOT PERMISSION.</h2><div class="steps"><div class="step"><span class="n">01 / REQUEST</span>The agent sends recipient, amount, category, memo and a unique idempotency key.</div><div class="step"><span class="n">02 / POLICY</span>The policy engine allows, denies, or creates an approval request with reasons.</div><div class="step"><span class="n">03 / APPROVAL</span>The owner approves or rejects. Only an approved request is eligible for execution.</div></div><p>A terminal status is immutable. Repeating the same request with the same idempotency key returns the original result rather than creating another payment. Approval expiry and executor failures fail closed.</p><div class="code"><label>curl: request a simulated payment</label><pre>curl -X POST https://patronhill.ru/v1/payments/request \\
  -H "Authorization: Bearer guardrail-demo-agent" \\
  -H "Content-Type: application/json" \\
  -H "Idempotency-Key: lunch-001" \\
  -d '{"recipient":{"address":"EQDemoMerchant"},"amount":{"value":8.50,"currency":"USD"},"category":"groceries","memo":"demo"}'</pre></div><div class="code"><label>TypeScript fetch</label><pre>const response = await fetch("https://patronhill.ru/v1/payments/request", {
  method: "POST",
  headers: { Authorization: "Bearer guardrail-demo-agent", "Content-Type": "application/json", "Idempotency-Key": "lunch-001" },
  body: JSON.stringify({ recipient: { address: "EQDemoMerchant" }, amount: { value: 8.5, currency: "USD" }, category: "groceries" })
});
const payment = await response.json();</pre></div></section>
<section id="architecture"><h2>SMALL, SEPARATED TRUST ZONES.</h2><p>The agent-facing API accepts a narrow payment intent. The policy engine evaluates that intent against an owner-managed policy. The approval surface is a separate owner capability. Finally, an executor receives only an approved execution command. This separation makes it practical to swap a stub for a real connector without widening the agent's authority.</p><div class="table-wrap"><table class="table"><thead><tr><th>Component</th><th>Responsibility</th><th>Must not hold</th></tr></thead><tbody><tr><td>Agent client</td><td>Create and read its payment requests</td><td>Owner token, wallet secret</td></tr><tr><td>Policy engine</td><td>Evaluate limits and rule reasons</td><td>Unbounded agent authority</td></tr><tr><td>Owner dashboard</td><td>Manage policy and approvals</td><td>Agent credential</td></tr><tr><td>Executor</td><td>Perform approved work only</td><td>Policy-edit authority</td></tr></tbody></table></div></section>
<section id="policies"><h2>POLICY IS THE PRODUCT.</h2><p>Policies express what is safe before an agent acts: per-payment and daily limits, permitted currencies, recipient allowlists, category restrictions, velocity windows, and a manual-review threshold. Use strict defaults: deny unknown categories, cap exposure, and require approval for first-time or high-value recipients.</p><div class="callout safe"><b>Recommended rollout:</b> begin with <code>EXECUTOR=stub</code>, log all requests, tune limits from observed behavior, then enable a production executor only after owner approvals and secret handling are tested.</div></section>
<section id="docker"><h2>RUN LOCALLY WITH DOCKER.</h2><p>Clone the project, create an environment file from the example, and keep execution stubbed while integrating. Use placeholders locally; do not commit secrets or insert a real mnemonic into an agent runtime.</p><div class="code"><label>local development</label><pre>git clone ${github}.git
cd guardrail-wallet
cp .env.example .env
docker compose up --build</pre></div><div class="table-wrap"><table class="table"><thead><tr><th>Variable</th><th>Example safe value</th><th>Purpose</th></tr></thead><tbody><tr><td>PORT</td><td>3000</td><td>HTTP listener</td></tr><tr><td>EXECUTOR</td><td>stub</td><td>Simulation; no settlement</td></tr><tr><td>AGENT_TOKEN</td><td>local-agent-token</td><td>Agent API authentication</td></tr><tr><td>OWNER_TOKEN</td><td>local-owner-token</td><td>Owner approval authentication</td></tr><tr><td>DATABASE_URL</td><td>file:./data/guardrail.db</td><td>Local persistent state</td></tr><tr><td>APP_BASE_URL</td><td>http://localhost:3000</td><td>Public links and callbacks</td></tr></tbody></table></div></section>
<section id="security"><h2>FAIL CLOSED, AUDIT ALWAYS.</h2><p>Guardrail Wallet treats credentials as capabilities. Keep agent and owner tokens separate, use least privilege, rotate tokens, and store production secrets only in a secret manager. Every decision should have an audit record containing the request, policy version, decision, approver, timestamp, and execution outcome. A missing policy, expired approval, invalid amount, or unavailable executor must deny or fail the request rather than guess.</p></section>
<section id="troubleshooting"><h2>TROUBLESHOOTING.</h2><h3>My request is denied</h3><p>Read the returned policy reasons. Check currency, amount, category, recipient rules, and rolling limits. Do not loosen every rule at once; adjust the smallest rule that explains the intended use case.</p><h3>My request stays pending</h3><p>It needs an owner action or has an approval window. Open <a href="/dashboard">Dashboard</a> with an owner credential, approve or reject it, and submit a new request if it expired.</p><h3>I expected money to move</h3><p>With <code>EXECUTOR=stub</code>, it cannot. This is expected for the demo and recommended during integration. Configure a production executor only with reviewed operational controls.</p></section>
</article></div></main>`);

export const docsRussianHtml: string = chrome("ru", "Документация Guardrail Wallet | Платежный firewall для AI-агентов", "Русское руководство Guardrail Wallet: безопасный жизненный цикл платежа AI-агента, политики и согласования.", "https://patronhill.ru/docs/ru", `<main>
<header class="shell hero"><div class="hero-grid"><div><span class="eyebrow"><b></b>Документация / RU</span><h1>АГЕНТ<br>ПРОСИТ.<br>ВЛАДЕЛЕЦ РЕШАЕТ.</h1><p>Guardrail Wallet - платежный firewall для AI-агентов. Агент формирует запрос, политика оценивает риск, а владелец подтверждает нестандартные операции. Это не кошелек, выданный агенту без ограничений.</p></div><aside class="hero-card"><strong>Публичная песочница</strong>Агент: <code>guardrail-demo-agent</code><br>Владелец: <code>guardrail-demo-owner</code><br><br>Демо работает с <code>EXECUTOR=stub</code>. Это имитация исполнения: реальных платежей, переводов и списаний нет.</aside></div></header>
<div class="shell layout"><aside class="toc"><b>Разделы</b><a href="#demo">Как работает demo</a><a href="#flow">Жизненный цикл</a><a href="#policy">Политики</a><a href="#arch">Архитектура</a><a href="#local">Docker локально</a><a href="#safe">Безопасность</a><a href="#help">Проблемы</a></aside><article class="content">
<section id="demo"><h2>DEMO - ЭТО НЕ БАНК.</h2><p>На <a href="/dashboard">dashboard</a> можно пройти весь сценарий с открытыми демонстрационными учетными данными. Токен агента нужен для создания запросов; токен владельца - для просмотра и решения по ожидающим запросам. Данные общедоступны и могут быть изменены другими посетителями. Не используйте личные данные или настоящие реквизиты.</p><div class="callout"><b>Главное ограничение:</b> в хостинге установлен <code>EXECUTOR=stub</code>. Статус <code>executed</code> в demo означает запись о симулированном результате, а не блокчейн-транзакцию и не банковский перевод.</div></section>
<section id="flow"><h2>ЗАПРОС - ЕЩЕ НЕ РАЗРЕШЕНИЕ.</h2><div class="steps"><div class="step"><span class="n">01 / REQUEST</span>Агент отправляет получателя, сумму, валюту, категорию, комментарий и idempotency key.</div><div class="step"><span class="n">02 / POLICY</span>Политика выносит allow, deny или требует approval с объяснимыми причинами.</div><div class="step"><span class="n">03 / APPROVAL</span>Владелец подтверждает либо отклоняет. Только approved запрос может попасть в executor.</div></div><p>Окончательный статус не меняется. Повтор того же idempotency key вернет исходный запрос и не создаст дубль. Истекшее согласование, ошибка executor или отсутствующая политика должны завершаться безопасным отказом.</p><div class="code"><label>curl: создать запрос в demo</label><pre>curl -X POST https://patronhill.ru/v1/payments/request \\
  -H "Authorization: Bearer guardrail-demo-agent" \\
  -H "Content-Type: application/json" \\
  -H "Idempotency-Key: ru-demo-001" \\
  -d '{"recipient":{"address":"EQDemoMerchant"},"amount":{"value":8.50,"currency":"USD"},"category":"groceries"}'</pre></div><div class="code"><label>TypeScript fetch</label><pre>const result = await fetch("https://patronhill.ru/v1/payments/request", {
  method: "POST",
  headers: { Authorization: "Bearer guardrail-demo-agent", "Content-Type": "application/json", "Idempotency-Key": "ru-demo-001" },
  body: JSON.stringify({ recipient: { address: "EQDemoMerchant" }, amount: { value: 8.5, currency: "USD" }, category: "groceries" })
}).then(r =&gt; r.json());</pre></div></section>
<section id="policy"><h2>ПОЛИТИКА ОГРАНИЧИВАЕТ УЩЕРБ.</h2><p>Политика задает разрешенные валюты и категории, лимит на одну операцию, дневной лимит, окна скорости, allowlist получателей, порог ручного подтверждения и правила для новых адресов. Пишите ее от запрета к исключениям: неизвестная категория должна быть запрещена, а крупный или новый получатель - требовать approval.</p><div class="table-wrap"><table class="table"><thead><tr><th>Сигнал</th><th>Типичное решение</th><th>Зачем</th></tr></thead><tbody><tr><td>Новая категория</td><td>deny</td><td>Нет молчаливого расширения полномочий</td></tr><tr><td>Сумма выше порога</td><td>approval_required</td><td>Человек видит контекст</td></tr><tr><td>Повторный idempotency key</td><td>return_existing</td><td>Retry не дублирует операцию</td></tr><tr><td>Нет активной политики</td><td>deny</td><td>Система не гадает</td></tr></tbody></table></div></section>
<section id="arch"><h2>РАЗДЕЛЕНИЕ ПОЛНОМОЧИЙ.</h2><p>Agent API принимает только намерение платежа. Policy engine принимает решение. Owner dashboard управляет правилами и согласованиями. Executor получает команду только после разрешения. Агентский токен не дает доступа к настройкам владельца и не должен содержать ключи расчета.</p><div class="callout safe"><b>Практический порядок:</b> сначала интеграция со <code>stub</code>, затем анализ журналов и настройка политик, потом отдельный production executor с защищенными секретами и контролем владельца.</div></section>
<section id="local"><h2>ЛОКАЛЬНЫЙ ЗАПУСК В DOCKER.</h2><div class="code"><label>development</label><pre>git clone ${github}.git
cd guardrail-wallet
cp .env.example .env
docker compose up --build</pre></div><p>Безопасные значения для локального <code>.env</code>: <code>PORT=3000</code>, <code>EXECUTOR=stub</code>, <code>AGENT_TOKEN=local-agent-token</code>, <code>OWNER_TOKEN=local-owner-token</code>, <code>DATABASE_URL=file:./data/guardrail.db</code>, <code>APP_BASE_URL=http://localhost:3000</code>. Это примеры, не реальные секреты. Production tokens и ключи храните только в менеджере секретов.</p></section>
<section id="safe"><h2>МОДЕЛЬ БЕЗОПАСНОСТИ.</h2><p>Учетные данные - это capability. Разделяйте токены агента и владельца, минимизируйте их права, регулярно ротируйте, не передавайте секреты в prompt и не храните их в репозитории. Журнал должен связывать запрос, версию политики, решение, человека, время и результат исполнения. Отсутствие данных, ошибка интеграции или timeout должны приводить к fail closed.</p></section>
<section id="help"><h2>ЕСЛИ ЧТО-ТО НЕ ТАК.</h2><h3>Запрос отклонен</h3><p>Проверьте причины в ответе API: сумму, валюту, категорию, получателя и накопленный лимит. Корректируйте конкретное правило, а не отключайте все ограничения.</p><h3>Запрос pending</h3><p>Требуется решение владельца в <a href="/dashboard">Dashboard</a> либо срок approval истек. После expiration создайте новый запрос с новым ключом.</p><h3>Нет настоящего перевода</h3><p>Это ожидаемо при <code>EXECUTOR=stub</code>. В demo и на этапе интеграции реальные платежи намеренно отключены.</p></section>
</article></div></main>`);

export const apiDocsHtml: string = chrome("en", "Guardrail Wallet API Reference | Agent payment firewall", "API reference for Guardrail Wallet payment requests, policy decisions, approvals, statuses, errors and idempotency.", "https://patronhill.ru/api", `<main>
<header class="shell hero"><div class="hero-grid"><div><span class="eyebrow"><b></b>API reference / v1</span><h1>ONE INTENT.<br>THREE<br>OUTCOMES.</h1><p>Use the Guardrail Wallet API to submit bounded payment requests. The service returns an explicit policy outcome: allowed, denied, or awaiting an owner approval.</p></div><aside class="hero-card"><strong>Base URL</strong><code>https://patronhill.ru/v1</code><br><br>Demo bearer: <code>guardrail-demo-agent</code><br>Demo owner: <code>guardrail-demo-owner</code><br><br><code>EXECUTOR=stub</code>: no real payments.</aside></div></header>
<div class="shell layout"><aside class="toc"><b>API reference</b><a href="#auth">Authentication</a><a href="#request">Create request</a><a href="#read">Read request</a><a href="#approval">Approvals</a><a href="#status">Statuses</a><a href="#errors">Errors</a><a href="#idempotency">Idempotency</a></aside><article class="content">
<section id="auth"><h2>AUTHENTICATION.</h2><p>Send the agent credential as a bearer token for payment-request operations. Owner approval operations require the owner credential. Keep them separate: an agent token must not approve, alter policies, or access execution secrets.</p><div class="code"><label>headers</label><pre>Authorization: Bearer guardrail-demo-agent
Content-Type: application/json
Idempotency-Key: client-generated-unique-key</pre></div></section>
<section id="request"><h2><span class="pill">POST</span> /PAYMENTS/REQUEST</h2><p>Creates a payment intent and evaluates active policy. Include a unique <code>Idempotency-Key</code> for every logical payment. Amounts are decimal values and the recipient address is treated as opaque by the firewall.</p><div class="code"><label>request body</label><pre>{
  "recipient": { "address": "EQDemoMerchant", "label": "Demo merchant" },
  "amount": { "value": 8.50, "currency": "USD" },
  "category": "groceries",
  "memo": "agent purchase"
}</pre></div><div class="code"><label>201 response</label><pre>{
  "id": "pay_01H...",
  "status": "approval_required",
  "policy": { "decision": "approval_required", "reasons": ["amount requires owner approval"] },
  "idempotency_key": "client-generated-unique-key",
  "created_at": "2026-07-27T12:00:00Z"
}</pre></div></section>
<section id="read"><h2><span class="pill">GET</span> /PAYMENTS/:ID</h2><p>Returns the current request, policy decision, approval history, and execution result when available. Agent callers should only retrieve requests within their own capability scope; owner callers may inspect the approval queue in <a href="/dashboard">Dashboard</a>.</p></section>
<section id="approval"><h2><span class="pill">POST</span> /PAYMENTS/:ID/APPROVE</h2><p>Owner-only operation. Approving a request transitions it to execution eligibility; rejecting it is terminal. An expired, denied, already executed, or unknown request cannot be approved.</p><div class="code"><label>curl: approve as owner</label><pre>curl -X POST https://patronhill.ru/v1/payments/pay_01H/approve \\
  -H "Authorization: Bearer guardrail-demo-owner" \\
  -H "Content-Type: application/json" \\
  -d '{"note":"Reviewed in demo"}'</pre></div><div class="callout">The hosted demo executor is <code>stub</code>. Approval demonstrates the control flow only; it never triggers a real transfer.</div></section>
<section id="status"><h2>STATUSES AND TRANSITIONS.</h2><div class="table-wrap"><table class="table"><thead><tr><th>Status</th><th>Meaning</th><th>Next action</th></tr></thead><tbody><tr><td><code>allowed</code></td><td>Policy permitted the request</td><td>Executor may process it</td></tr><tr><td><code>approval_required</code></td><td>Policy requires owner decision</td><td>Owner approves or rejects</td></tr><tr><td><code>approved</code></td><td>Owner approved</td><td>Executor processes or fails closed</td></tr><tr><td><code>denied</code></td><td>Policy rejected the request</td><td>Terminal; inspect reasons</td></tr><tr><td><code>executed</code></td><td>Executor reported success</td><td>Terminal; stub in demo</td></tr><tr><td><code>failed</code></td><td>Execution could not complete</td><td>Terminal; investigate safely</td></tr><tr><td><code>rejected</code></td><td>Owner rejected the request</td><td>Terminal</td></tr></tbody></table></div></section>
<section id="errors"><h2>ERRORS ARE EXPLICIT.</h2><div class="table-wrap"><table class="table"><thead><tr><th>HTTP</th><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td>400</td><td><code>validation_error</code></td><td>Malformed recipient, amount, category, or body</td></tr><tr><td>401</td><td><code>unauthorized</code></td><td>Missing or invalid bearer credential</td></tr><tr><td>403</td><td><code>forbidden</code></td><td>Credential lacks owner or request scope</td></tr><tr><td>404</td><td><code>not_found</code></td><td>Payment request does not exist</td></tr><tr><td>409</td><td><code>idempotency_conflict</code></td><td>Key reused with a different request body</td></tr><tr><td>409</td><td><code>invalid_transition</code></td><td>Action does not match current lifecycle state</td></tr><tr><td>422</td><td><code>policy_denied</code></td><td>Policy refused the intent; inspect reasons</td></tr><tr><td>503</td><td><code>executor_unavailable</code></td><td>Execution unavailable; do not assume a payment happened</td></tr></tbody></table></div><div class="code"><label>error envelope</label><pre>{ "error": { "code": "policy_denied", "message": "Request exceeds daily limit", "request_id": "req_..." } }</pre></div></section>
<section id="idempotency"><h2>RETRIES MUST BE BORING.</h2><p>Generate one opaque idempotency key per logical payment and reuse it only when retrying the same request. A matching retry returns the first result; a changed payload with the same key returns <code>409 idempotency_conflict</code>. Store both your key and returned payment ID in your agent's durable state. Do not interpret a timeout as permission to generate another key.</p><div class="callout safe"><b>Production note:</b> begin with <code>EXECUTOR=stub</code> in local Docker, use placeholder environment values, and add a real executor only behind owner-controlled deployment and audited secret management.</div><p>Source and deployment details: <a href="${github}">GitHub</a>. For integration support or project funding, TON address: <code>${supportAddress}</code>.</p></section>
</article></div></main>`);
